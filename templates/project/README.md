# FLOW Project Contour

This template installs FLOW inside a single repository.

- `targets[0]` points at the current repository.
- `autonomy.mode` defaults to `supervised`.
- `shell.exec` is not enabled by default.
- approvals are required for `git.commit` and `shell.exec`.
